# TerraBattle 개발 완성 계획

## 1. 현재 개발 수준 진단

현재 저장소는 **게임 완성 전 단계의 전투 프로토타입** 수준이다.
단순 타이틀 화면만 있는 상태는 아니며, 실제로는 다음 요소가 이미 구현되어 있다.

### 현재 구현됨

- React 기반 `TitleScreen`과 씬 전환 루트
- PixiJS 기반 `MapScene`
- 가상 해상도 스테이지(`1080x1920`)와 반응형 fit 처리
- 맵 JSON 기반 초기 배치 로딩
- 아군 유닛 드래그 및 위치 변경
- 충돌 판정 기반 swap / 이동 처리
- 턴 타이머 HUD
- 턴 종료 시 샌드위치 공격 판정
- 스킬 발동 확률, 명중/회피, 피해 계산의 일부 도메인 로직
- 샌드위치 공격 / 스킬명 / 데미지 텍스트 연출 큐
- 핵심 전투 계산 유스케이스 단위 테스트 일부

### 현재 부족함

- 적 턴 행동 로직이 없음
- 승패 이후 흐름(결과 화면, 리트라이, 다음 스테이지) 미완성
- 전투 규칙이 샌드위치 + 데미지 계산 중심으로만 구현됨
- 버프/디버프/회복/상태이상/사거리/속성 등 핵심 RPG 규칙 미구현
- 유닛 상세 UI, HP/UI 피드백, 선택/타겟 정보 부족
- 스테이지 진행 구조, 저장, 성장, 보상 루프 부재
- 데이터 검증, 콘텐츠 제작 파이프라인, 운영 도구 부재
- 사운드, 연출, 튜토리얼, 설정, QA 체계 미구축

### 현재 단계 판정

**현재 단계는 “전투 코어 검증용 프로토타입”이다.**
상용/배포 가능한 게임이라기보다는, 핵심 퍼즐 전투 감각을 실험하는 단계로 보는 것이 맞다.

---

## 2. 목표 재정의

이 프로젝트의 우선 목표는 다음 두 가지 중 하나로 명확히 정해야 한다.

### 목표 A. 플레이 가능한 데모 완성

짧은 전투 3~5개를 연속으로 플레이 가능하고,
승패/재시작/기본 성장만 포함한 **데모 빌드**를 완성한다.

### 목표 B. 상용 수준의 최소 완성본(MVP) 완성

콘텐츠, 밸런스, 저장, 성장, UI/UX, 사운드, QA, 배포까지 포함한
**출시 가능한 최소 버전**을 완성한다.

현재 코드베이스 기준으로는 **A를 먼저 달성한 뒤 B로 확장**하는 순서가 가장 현실적이다.

---

## 3. 완성 기준 정의

`Plan.md`에서는 “게임 완성”을 아래 조건을 만족하는 상태로 정의한다.

### 필수 완성 조건

- 타이틀 → 전투 진입 → 승패 판정 → 결과 화면 → 다음 행동 흐름이 끊기지 않는다.
- 적 턴이 규칙적으로 동작한다.
- 유닛 HP, 상태, 턴 정보가 UI로 명확히 보인다.
- 전투 규칙이 플레이어가 학습 가능한 수준으로 정리된다.
- 최소 3개 이상의 스테이지가 존재한다.
- 최소 6~10종의 플레이어 유닛/적 유닛/스킬 조합이 제공된다.
- 저장 또는 세션 진행 유지 방식이 존재한다.
- 게임 오버 / 재시작 / 스테이지 클리어 루프가 완성된다.
- 타입 안정성과 테스트가 핵심 도메인에 대해 확보된다.
- 빌드가 안정적으로 재현 가능하다.

---

## 4. 우선순위 원칙

개발 우선순위는 아래 순서를 고정한다.

1. **전투 코어 완성**
2. **플레이 루프 완성**
3. **콘텐츠 확장**
4. **사용성/연출 강화**
5. **운영/품질 체계화**

즉, 아트/사운드/부가 시스템보다 먼저 **전투가 반복 가능하고 규칙적으로 재미있어야 한다.**

---

## 5. 단계별 개발 계획

## Phase 1. 전투 코어 안정화

### 목표

현재 `MapScene` 중심의 프로토타입을 실제 게임 규칙을 담을 수 있는 구조로 재정리한다.

### 해야 할 일

- 전투 상태를 UI 상태와 분리해 도메인 중심으로 재구성
- 아군 턴 / 적 턴 / 연출 재생 / 결과 처리 상태 머신 정의
- 적 AI 기본 규칙 구현
  - 이동 우선순위
  - 공격 우선순위
  - 샌드위치 성립 시도
- HP 감소 / 사망 / 제거 / 턴 종료 순서 명문화
- 버프 / 디버프 / 회복 / 상태이상 적용 구조 추가
- 스킬 해석 계층 분리
  - 발동 조건
  - 대상 선택
  - 효과 계산
  - 연출 이벤트 생성
- 맵/캐릭터/스킬 JSON 로딩 검증 추가
- 전투 로그 또는 디버그 이벤트 출력 도입

### 완료 기준

- 전투 규칙이 코드상 명확히 분리된다.
- 적 턴이 임시 자동 종료가 아니라 실제 행동한다.
- 전투 결과가 예측 가능하고 테스트 가능하다.

---

## Phase 2. 플레이어 전투 UX 완성

### 목표

플레이어가 “왜 이겼고 왜 졌는지” 이해할 수 있는 전투 UI를 만든다.

### 해야 할 일

- 유닛별 HP 바 표시
- 유닛 선택/호버 정보 패널
- 턴 상태, 남은 시간, 현재 진영 강조 개선
- 이동 가능 / 위험 / 공격 예고 시각화
- 스킬 발동, miss, 치명타, 버프/디버프 아이콘 표시
- 전투 결과 팝업(승리/패배)
- 재시도 / 타이틀 복귀 / 다음 스테이지 버튼
- 입력 잠금 상태와 연출 재생 상태의 UX 일관화

### 완료 기준

- 전투 중 핵심 정보가 UI에서 모두 확인 가능하다.
- 플레이어가 외부 설명 없이 기본 규칙을 이해할 수 있다.

---

## Phase 3. 데모 플레이 루프 완성

### 목표

한 번 실행한 사용자가 최소 10~20분 동안 끊기지 않고 플레이 가능한 데모를 만든다.

### 해야 할 일

- 스테이지 선택 또는 순차 진행 구조 구현
- 최소 3개 이상의 스테이지 데이터 제작
- 승리 시 다음 스테이지 진입
- 패배 시 재도전 루프 구현
- 간단한 성장 요소 도입
  - 레벨/강화 중 1개만 우선 선택
- 세션 저장 방식 결정
  - 로컬 스토리지 또는 세이브 슬롯
- 튜토리얼 메시지/첫 전투 안내 추가

### 완료 기준

- 타이틀부터 시작해 여러 전투를 연속 플레이할 수 있다.
- 진행 손실 없이 최소한의 세션 유지가 가능하다.

---

## Phase 4. 콘텐츠 확장

### 목표

전투 시스템이 반복 플레이를 버틸 수 있도록 콘텐츠 다양성을 확보한다.

### 해야 할 일

- 플레이어 유닛 역할군 정의
  - 탱커
  - 딜러
  - 서포터
  - 디스럽터
- 적 AI 패턴 유형 추가
  - 돌진형
  - 회피형
  - 원거리형
  - 지원형
- 스킬군 확장
  - 단일 공격
  - 다단 히트
  - 범위 효과
  - 버프/디버프
  - 회복
  - 반응형 패시브
- 스테이지 기믹 추가
  - 장애물
  - 진입 금지 칸
  - 턴 제한 목표
  - 보호 대상
- 난이도 곡선 설계
- 수치 밸런스 표 작성

### 완료 기준

- 조합에 따라 공략 방식이 달라진다.
- 동일 규칙 반복이 아니라 전술 선택이 발생한다.

---

## Phase 5. 기술 구조 정리

### 목표

프로토타입 코드를 유지보수 가능한 게임 구조로 전환한다.

### 해야 할 일

- `scene`, `game`, `shared`, `assets` 경계 재정리
- `MapScene` 대형 컴포넌트 분해
  - 입력 처리
  - 렌더링
  - HUD
  - 연출 레이어
  - 턴 제어
- 도메인 계층 분리
  - 엔티티
  - 서비스
  - 유스케이스
  - 리포지토리/로더
- 테스트 가능한 순수 함수 추출 확대
- JSON 스키마 검증 도입
- 에셋 레지스트리 및 로딩 정책 정리
- 에러 처리 및 fallback 정책 정의

### 완료 기준

- 핵심 파일이 과도하게 비대하지 않다.
- 전투 규칙 변경 시 UI 코드 수정 범위가 제한된다.

---

## Phase 6. 품질 보증 체계 구축

### 목표

기능 추가 속도보다 회귀 방지가 우선인 상태를 만든다.

### 해야 할 일

- 핵심 도메인 테스트 확대
  - 샌드위치 판정
  - 스킬 발동
  - 명중/회피
  - 사망 처리
  - 턴 전환
- JSON fixture 기반 시나리오 테스트 작성
- 주요 UI 상호작용 테스트 도입
- 타입체크 / 테스트 / 빌드 스크립트 점검
- CI 파이프라인 구성
- 릴리스 체크리스트 작성

### 완료 기준

- 핵심 전투 규칙은 테스트 없이 수정하지 않는 상태가 된다.
- PR/커밋 단위로 자동 검증이 가능하다.

---

## Phase 7. 연출 및 폴리시

### 목표

게임으로서의 완성도를 높이되, 코어 규칙 이후에만 투자한다.

### 해야 할 일

- BGM / SFX 추가
- 타격, 이동, 승리/패배 이펙트 강화
- 화면 전환 연출
- 타이틀 및 결과 화면 폴리시
- 설정 메뉴
  - 사운드
  - 해상도 대응 정책
  - 입력 옵션
- 접근성 검토
  - 색상 대비
  - 폰트 크기
  - 애니메이션 과다 여부

### 완료 기준

- 저비용 연출만으로도 플레이 감각이 분명히 개선된다.
- 시각/청각 피드백이 전투 판단을 방해하지 않는다.

---

## 6. 권장 마일스톤

## M1. 전투 시스템 알파

### 범위

- 적 AI 추가
- 전투 상태 머신 추가
- HP/사망/턴 종료 정리
- 전투 결과 판정 안정화

### 산출물

- 1개 스테이지에서 아군/적 턴이 정상 반복되는 빌드

## M2. 플레이어블 데모

### 범위

- 전투 HUD 강화
- 결과 화면
- 3개 스테이지
- 재시도 / 다음 스테이지 / 저장 일부

### 산출물

- 외부 사용자에게 보여줄 수 있는 데모 빌드

## M3. 콘텐츠 베타

### 범위

- 유닛/스킬/적 패턴 확장
- 성장/보상 구조 도입
- 난이도 조정

### 산출물

- 30분 이상 플레이 가능한 내부 테스트 빌드

## M4. MVP 릴리스 후보

### 범위

- QA / 밸런스 / 사운드 / 설정 / CI / 배포 준비

### 산출물

- 배포 가능한 최소 완성본

---

## 7. 즉시 실행 권장 백로그

다음 작업은 지금 코드베이스에서 가장 먼저 처리하는 것이 합리적이다.

### 최우선

1. `MapScene`에서 적 턴 임시 종료 로직 제거
2. 적 AI 1종 구현
3. HP 표시 및 사망 처리 UI 명시화
4. 전투 상태 머신 도입
5. 전투 결과 화면 추가

### 그 다음

6. 스테이지 전환 구조 추가
7. 저장 방식(LocalStorage) 도입
8. 스킬 효과 타입 확장(heal/buff/debuff)
9. 테스트 범위 확대
10. 맵/캐릭터/스킬 JSON 검증 추가

---

## 8. 예상 리스크

### 구조 리스크

- 현재 `MapScene`에 전투 규칙, 입력, 렌더링, 연출, HUD가 강하게 결합되어 있다.
- 기능 추가가 계속되면 수정 비용이 급격히 증가할 가능성이 높다.

### 콘텐츠 리스크

- 시스템보다 콘텐츠 생산 속도가 늦으면 데모 완성이 지연된다.
- 특히 스테이지 설계와 밸런싱은 코드보다 시간이 더 많이 든다.

### 품질 리스크

- 전투 규칙이 복잡해질수록 테스트 없는 수정은 회귀를 유발한다.
- JSON 데이터 증가 시 런타임 오류 가능성이 커진다.

### 일정 리스크

- 사운드/연출/UI 폴리시를 너무 일찍 잡으면 코어 완성이 지연된다.
- 먼저 재미있는 전투를 고정하지 않으면 후반 작업이 전부 재작업될 수 있다.

---

## 9. 결론

현재 프로젝트는 **버려야 할 수준은 아니고, 전투 코어 프로토타입으로서 충분히 의미 있는 상태**다.
다만 “게임 완성”을 목표로 할 경우, 다음 순서를 반드시 지켜야 한다.

1. 전투 규칙 완성
2. 적 AI 완성
3. 승패 포함 플레이 루프 완성
4. 콘텐츠 확장
5. 연출/품질/배포 정리

가장 중요한 판단은 하나다.

**지금은 콘텐츠를 늘릴 시점이 아니라, 전투 루프를 끝까지 닫을 시점이다.**
